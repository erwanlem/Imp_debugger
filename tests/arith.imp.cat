
function g() {
  var g;
  
  g = (10 + 10);
  return(g);
}

function h() {
  var h;
  
  h = 30;
  return(h);
}

function f() {
  
  return((g() + h()));
}

function main() {
  var t;
  var i;
  var a;
  var j;
  var k;
  var l;
  
  t = {(f() + g()); g(); (f() + 12); };
  a = (f() + g());
  i = 0;
  j = 1;
  k = 2;
  l = 3;
  i = f();
}


